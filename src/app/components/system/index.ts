/**
 * Curated public surface for the internal UI system.
 *
 * Author new shared UI in `src/app/components/primitives` or
 * `src/app/components/patterns`, then re-export stable pieces here.
 */
export * as systemPatterns from './patterns';
export * as systemPrimitives from './primitives';
export * as systemTokens from './tokens';
