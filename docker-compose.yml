services:
  navet:
    build:
      context: .
      dockerfile: Dockerfile
    image: navet:local
    ports:
      - "8080:80"
    env_file:
      - .env
    volumes:
      - navet-data:/data
    restart: unless-stopped

volumes:
  navet-data:
