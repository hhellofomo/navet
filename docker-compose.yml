services:
  navet:
    build:
      context: .
      dockerfile: Dockerfile
    image: navet:local
    env_file:
      - .env
    ports:
      - "8080:80"
    volumes:
      - navet-data:/data
    restart: unless-stopped

volumes:
  navet-data:
