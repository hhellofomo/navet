services:
  navet:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        NAVET_ENABLE_DEMO: "false"
    image: navet:local
    ports:
      - "8082:80"
    env_file:
      - .env
    volumes:
      - navet-data:/data
    restart: unless-stopped

volumes:
  navet-data:
